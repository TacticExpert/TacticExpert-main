Run the following command to download the meta data from [Kaggle](https://www.kaggle.com/datasets/deepsportradar/basketball-instants-dataset) and place it in the `./src/data` folder:

```bash
cd ./src/data
kaggle datasets download deepsportradar/basketball-instants-dataset
unzip ./basketball-instants-dataset.zip -d .
```